id: BE-TASK-5
title: "Implement Class APIs (Cluster 3 - APIs #12-#19)"
assignee: "@be-secondary"
status: "In Progress"
priority: "High"
description: "Implement the 8 class/class-membership endpoints per .agents/private/plans/class_api_implementation_plan.md on branch feat/class-api. Phases 1-3 (APIs #12-#19) complete with unit tests; Phase 4 (integration/hardening/contract) complete: new ClassApiIntegrationTest (@SpringBootTest + @AutoConfigureMockMvc + @Transactional, real local Postgres, real JWT via JwtTokenService) covers lifecycle flow (#13->#15->#18->#17->#19->#16 409->#19->#16 200->#14 404), role scoping #12 (ADMIN/TEACHER-A/TEACHER-B/member STUDENT/non-member STUDENT), status cycle + invalid 400, teacher-change #15, error-contract spot checks (exact messages, pagination present in #12 and absent in #17, LocalDate ISO '2026-09-15', name 151 -> 400, endDate<startDate -> 400 create+update, teacher-null class persisted). 8 integration tests passing; full suite 126 tests green. Contract notes delivered to production_artifacts/be_to_docs/class-api-contract-notes.md; runbook to production_artifacts/be_to_tester/class-api-testing-instructions.md. Decisions (final): role checks skipped (security layer owns 401/403; only #12 data-scoping in service); module-owned persistence; infra ClassStatus enum reused; open items A-E resolved as defaults (A date-range 400; B teacher-404 on update; C empty-update 400; D delete counts class_members+assignments+student_evaluations; E service-level duplicate check ONLY — NO unique index on (class_id,student_id), so concurrent #18 duplicate returns 500 via GlobalExceptionHandler; #16 delete has same-transaction check, residual race risk at READ_COMMITTED acknowledged); #18 201 includes memberId (confirmed); #17 excludes soft-deleted students (confirmed); integration tests run against shared local appdb in default ./gradlew test."