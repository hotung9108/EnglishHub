package com.english_hub.core.modules.submission.domain.model;

/** Submission-context duplicate of the shared module skill enum. */
public enum ModuleSkill {
	READING,
	LISTENING,
	WRITING,
	SPEAKING
}