package com.english_hub.core.modules.submission.domain.model;

/** Lifecycle of a student submission and of each submission-module row. */
public enum SubmissionStatus {
	IN_PROGRESS,
	SUBMITTED,
	GRADED
}