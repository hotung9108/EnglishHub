package com.english_hub.core.modules.submission.domain.model;

/** Submission-context duplicate of the shared module task type enum. */
public enum ModuleTaskType {
	QUIZ,
	REWRITE,
	RECORDING,
	ESSAY
}